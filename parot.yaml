proxy:
  hostname: localhost
  port: 8087
  persist:
    type: file
    file:
      path: ./db
    database:
      type: postgres
      name: mydb
      hostname: localhost
      port: 8081
      user: mydb_user
      password: password
  log:
    path: ./logs
    level: debug